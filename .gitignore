# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.DS_Store
.idea/
.vscode/
._*
.AppleDouble/
.Spotlight-V100
.Trashes

# Secrets — never commit
.env
.env.*
*.pem
*.key
*.p12
*.pfx
secrets.yaml
factura_*.pdf
facturas*/
*PRIVADAS*/

# Build artifacts
*.zip
dist/
build/

# HA local dev artifacts
config/
.storage/
home-assistant.log
home-assistant_v2.db*

# Madrileña-specific dev artifacts (NEVER ship — contain real session
# cookies, real meter readings, real HTML responses).
cookies.json
probe.log
probe.stdout.log
probe_session.py
fetch_consumos.py
snapshot_history.py
history_snapshot.json
snapshots/
responses/

# Tests reference real-data fixtures (history_snapshot.json,
# responses/). Excluded from v0.1.0; will be rewritten with
# synthetic fixtures in a follow-up.
tests/
